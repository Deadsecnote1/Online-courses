rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public and admin traffic go through Next.js API routes (Admin SDK).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
